# FastAPI and core dependencies
fastapi>=0.141,<0.142
uvicorn[standard]==0.24.0
# Pinned to ~=0.28 for _pool private-API compatibility in src/shared/url_safety.py's
# _build_pinned_transport (SSRF guard IP pinning). Re-verify on upgrade — see CONTRIBUTING.md.
httpx~=0.28
aiohttp>=3.14.3,<4
pydantic>=2.13,<3
pydantic-settings>=2.1.0,<3.0
email-validator==2.1.0.post1
structlog==23.2.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.12.1

# Authentication and security
authlib>=1.8,<2
python-jose[cryptography]>=3.5,<4
python-multipart>=0.0.32
itsdangerous==2.1.2

# Session and caching
redis==5.0.1
starsessions[redis]==2.1.3

# Additional utilities
python-dotenv>=1.2.3,<2

# Wyoming protocol for voice services (STT/TTS)
wyoming==1.8.0

# Twilio for SMS messaging
twilio>=9.0.0

# iCal calendar parsing
icalendar>=5.0.0

# Qdrant vector database client
# Pin to match server version in manifests/athena-prod/qdrant.yaml (server v1.18.2).
# Client minor must be <= server minor (client 1.18.x is compatible with server 1.18.x).
# Verified latest: qdrant-client 1.18.0 on PyPI (https://pypi.org/project/qdrant-client/).
# When the server image is bumped, update this pin in the same commit.
qdrant-client~=1.18.0

# FastEmbed for lightweight embeddings (used by memory system)
# Much lighter than sentence-transformers (~100MB vs ~3GB)
fastembed>=0.2.0

# test-only (Phase 3 OIDC fixture issuer; ATHENA-12)
pytest-httpserver>=1.0.8
# test-only (Phase 5b regression-guard HTML label check; ATHENA-11 C1)
beautifulsoup4>=4.12,<5

# Campaign 3 / ATHENA-14 — rate limiter for /local-login
fastapi-limiter==0.1.6
# test-only (Campaign 3 / ATHENA-14 — fastapi-limiter Lua scripts)
fakeredis[lua]>=2.21,<3
